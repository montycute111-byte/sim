// Optional local fallback for non-Vercel local testing.
// Keep null in production and use process.env.FIREBASE_CONFIG via /api/firebase-config.js.
window.FIREBASE_CONFIG = window.FIREBASE_CONFIG || null;
